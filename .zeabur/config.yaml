# Zeabur 部署配置
# 将此仓库连接到 Zeabur 后，会自动创建两个服务

# 后端服务配置
# Zeabur 会自动检测 backend/requirements.txt 和 main.py
# 默认启动命令: uvicorn main:app --host 0.0.0.0 --port $PORT

# 前端服务配置
# Zeabur 会自动检测 frontend/package.json
# 构建命令: npm run build:h5
# 输出目录: dist/build/h5
