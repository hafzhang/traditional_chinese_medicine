# 首页设计文档

## 页面概述

| 项目 | 说明 |
|------|------|
| 页面路径 | `/pages/index/index` |
| 页面名称 | 首页 - 中医体质识别 |
| 主要功能 | 展示产品介绍、九种体质类型、功能特点、科学依据 |
| 数据来源 | `@/data/constitution.js` |

## 页面结构

```
首页 (index)
├── Hero 区域
│   ├── 背景渐变
│   ├── 医疗图标：🩺
│   ├── 标题：中医体质识别
│   ├── 副标题：基于王琦院士 CCMQ 标准量表
│   └── CTA 按钮：开始体质测试
│
├── 测试说明卡片
│   ├── 图标：📋
│   ├── 标题：测试说明
│   └── 说明列表（3条）
│   │   ├── 30道科学问题
│   │   ├── 9种体质类型
│   │   └── AI 智能分析
│
├── 九种体质类型卡片
│   ├── 图标：☯
│   ├── 标题：九种体质类型
│   ├── 提示：点击查看详情
│   └── 体质网格 (3x3)
│       └── 体质卡片 x 9
│           ├── 头部（彩色）
│           │   ├── 图标
│           │   └── 名称
│           └── 操作：查看详情 →
│
├── 功能特点卡片
│   ├── 图标：⭐
│   ├── 标题：功能特点
│   └── 特点列表（3项）
│       ├── 图标（渐变背景）
│       ├── 标题
│       └── 描述
│
├── 科学依据卡片
│   ├── 图标：📚
│   ├── 标题：科学依据
│   └── 科学说明列表（3项）
│       ├── 图标
│       ├── 标题
│       └── 描述
│
└── 底部行动按钮
    └── CTA：立即开始测试
```

## 组件设计

### 1. Hero 区域 (`.hero-section`)

**样式规格**
```scss
.hero-section {
  position: relative;
  padding: 60rpx 30rpx 40rpx;
  overflow: hidden;
}

.hero-bg {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

**元素说明**
| 元素 | 类型 | 规格 |
|------|------|------|
| 图标 | Emoji | 120rpx |
| 标题 | 文本 | 52rpx, 粗体 |
| 副标题 | 文本 | 28rpx, 透明度0.9 |
| 标签 | 胶囊 | 10rpx 24rpx, 圆角30rpx |
| 按钮 | 主按钮 | 大尺寸, 全宽 |

### 2. 体质网格 (`.constitution-grid`)

**布局规格**
```scss
.constitution-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20rpx;
}
```

**体质卡片结构**
```
┌─────────────────────┐
│   [图标] 名称  │ ← 头部（彩色）
├─────────────────────┤
│ 描述文本...          │
│                     │
│ [特征1] [特征2]     │
│                     │
│      查看详情 →      │
└─────────────────────┘
```

**九种体质配色**

| 体质类型 | 颜色 | 图标 | 中文名称 |
|----------|------|------|----------|
| peace | #52c41a | ☯ | 平和质 |
| qi_deficiency | #faad14 | 气 | 气虚质 |
| yang_deficiency | #1890ff | 阳 | 阳虚质 |
| yin_deficiency | #eb2f96 | 阴 | 阴虚质 |
| phlegm_damp | #722ed1 | 痰 | 痰湿质 |
| damp_heat | #fa541c | 湿 | 湿热质 |
| blood_stasis | #f5222d | 瘀 | 血瘀质 |
| qi_depression | #13c2c2 | 郁 | 气郁质 |
| special | #52c41a | 特 | 特禀质 |

### 3. 功能特点 (`.feature-list`)

**特点列表数据**
```javascript
const features = [
  { icon: '📋', title: '科学量表', desc: '采用国家中医药管理局发布的CCMQ标准量表', bgColor: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)' },
  { icon: '🎯', title: '精准识别', desc: '通过算法精确计算，识别主要体质和次要体质', bgColor: 'linear-gradient(135deg, #f093fb 0%, #f5576c 100%)' },
  { icon: '🥗', title: '饮食推荐', desc: '根据体质类型提供个性化的饮食建议', bgColor: 'linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)' },
  { icon: '📊', title: '详细报告', desc: '生成详细的体质分析报告和调理建议', bgColor: 'linear-gradient(135deg, #43e97b 0%, #38f9d7 100%)' },
  { icon: '💡', title: '养生指导', desc: '包含运动、起居、情志等多方面养生建议', bgColor: 'linear-gradient(135deg, #fa709a 0%, #fee140 100%)' },
  { icon: '🔄', title: '可重复测试', desc: '随时重新测试，跟踪体质变化情况', bgColor: 'linear-gradient(135deg, #a8edea 0%, #fed6e3 100%)' }
]
```

**布局规格**
```scss
.feature-card {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 24rpx;
  background: #f8f9ff;
  border-radius: 16rpx;
}

.feature-icon {
  width: 80rpx;
  height: 80rpx;
  border-radius: 16rpx;
}
```

## 数据流

### 输入数据

从 `@/data/constitution.js` 导入：

```javascript
import { CONSTITUTION_INFO } from '@/data/constitution.js'

const constitutionTypes = ref(Object.values(CONSTITUTION_INFO).map(info => ({
  type: info.type,           // 体质类型标识
  name: info.name,           // 体质名称
  icon: info.icon,           // 体质图标
  color: info.color,         // 体质颜色
  shortDesc: info.description, // 简短描述
  features: info.characteristics.overall.slice(0, 2) // 前2条特征
})))
```

### 用户交互

| 交互元素 | 触发事件 | 目标页面/行为 |
|----------|----------|---------------|
| "开始体质测试" 按钮 | `@click="startTest"` | 跳转 `/pages/test/test` |
| "立即开始测试" 按钮 | `@click="startTest"` | 跳转 `/pages/test/test` |
| 体质卡片 | `@click="viewConstitution(item)"` | 跳转 `/pages/detail/detail?constitution={type}` |

## API 调用

无 API 调用，所有数据来自本地 `constitution.js`。

## 样式变量

### 颜色系统
```scss
// 主色调
$primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

// 体质颜色
$color-peace: #52c41a;
$color-qi_deficiency: #faad14;
$color-yang_deficiency: #1890ff;
$color-yin_deficiency: #eb2f96;
$color-phlegm_damp: #722ed1;
$color-damp_heat: #fa541c;
$color-blood_stasis: #f5222d;
$color-qi_depression: #13c2c2;
$color-special: #52c41a;

// 中性色
$bg-color: #f5f5f5;
$card-bg: #ffffff;
$text-primary: #1a1a1a;
$text-secondary: #666666;
$text-tertiary: #999999;
```

### 间距系统
```scss
$spacing-xs: 8rpx;
$spacing-sm: 12rpx;
$spacing-md: 20rpx;
$spacing-lg: 30rpx;
$spacing-xl: 40rpx;
```

### 圆角系统
```scss
$radius-sm: 12rpx;
$radius-md: 16rpx;
$radius-lg: 24rpx;
$radius-xl: 40rpx;
```

## 响应式设计

| 屏幕宽度 | 体质网格列数 |
|----------|--------------|
| < 600rpx | 2 列 |
| >= 600rpx | 3 列 |

## 可访问性

- 所有按钮具有足够的点击区域（最小 44x44 pt）
- 颜色对比度符合 WCAG AA 标准
- 语义化的 HTML 结构

## 性能优化

- 使用 `v-for` 的 `:key` 进行列表渲染优化
- 本地数据，无需网络请求
- CSS 渐变使用 GPU 加速

## 未来扩展

- [ ] 添加体质卡片动画效果
- [ ] 添加分享功能
- [ ] 添加用户历史记录入口
